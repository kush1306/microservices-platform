# Application
APP_NAME=User Service
APP_VERSION=1.0.0
DEBUG=false
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO

# Database (SQLite for local development — required)
DATABASE_URL=sqlite:///./users.db

# CORS (comma-separated origins, or * for all)
CORS_ORIGINS=*
